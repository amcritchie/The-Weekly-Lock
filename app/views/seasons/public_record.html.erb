<div class="container">

  <div class="row">
    <div class="input-group"> <span class="input-group-addon">Filter</span>
      <input id="filter" type="text" class="form-control" placeholder="Type here...">
    </div>

    <ul class="flex-container searchable">
      <% @picks.each do |pick| %>
      <% game = pick.performance.game %>

      <li class="flex-item game-box <%= current_user && current_user.games.find_by_id(game.id) ? 'pick' : '' %>">
        Week <%= game.week.position %>
        <%= render :template => "games/game_box", :locals => { :game => game, show_result: true, show_spread: true } %>
      </li>
      <% end %>
    </ul>
  </div>

</div>
